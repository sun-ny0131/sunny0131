# Set your email and name for Git
email <- "sunsunny0515@gmail.com"
name <- "sun-ny0131"

# Run the git config commands
system2("git", args = c("config", "--global", "user.email", email))
system2("git", args = c("config", "--global", "user.name", name))
